<Window x:Class="Finance.Pages.NewTransactionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Finance.Pages"
        mc:Ignorable="d"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Title="Adicionar Transação" Height="350" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <Label>Descrição:</Label>
                <TextBox x:Name="txtDescription" />
            </StackPanel>
            <StackPanel Grid.Row="1"  Margin="0,0,0,12">
                <Label>Valor:</Label>
                <TextBox x:Name="txtValue" />
            </StackPanel>
            <Grid Grid.Row="2" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Label Content="Tipo:" VerticalAlignment="Center" />
                <ComboBox x:Name="cmbType" Grid.Column="1" Height="25" VerticalAlignment="Center" VerticalContentAlignment="Center">
                    <ComboBoxItem Tag="Income">Entrada</ComboBoxItem>
                    <ComboBoxItem Tag="Outcome">Saída</ComboBoxItem>
                </ComboBox>
            </Grid>
            <DatePicker x:Name="dpDate" Grid.Row="3" Height="30" VerticalContentAlignment="Center" />
        </Grid>
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Cancelar" Click="Button_Click_1" />
            <Button Content="Adicionar" Click="Button_Click" />
        </StackPanel>

    </Grid>
</Window>
